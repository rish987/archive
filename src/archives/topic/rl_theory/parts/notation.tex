\begin{part}{Notation}
  To answer this question, let's establish some notation.

  Let's 
  \reflnenv{definition}{states}{define}{\statesdef{,}}
  and 
  \reflnenv{definition}{actions}{define}{\actionsdef{,}}
  where $\S$ and $\A$ are finite.
  \reflnenv{definition}{rewards}{Define}{\rewardsdefg{\refln{note}{fnnot}{$\rewardsdefnot$} $\defeq$}{.}}

  \refln{definition}{steplim}{Define}
  \steplimdef{,}
  \refln{definition}{str}{define}
  \strdef{,}
  and \refln{definition}{strs}{define}
  \strsdef{.}
  Note that \refln{proof}{npol}{$|\ST| = \T \times |\S| \times |\A|$}.

  \refln{definition}{inddef}{Define} \inddefdefg{\refln{note}{paramprob}{$\inddefp$} $\defeq$}{.}
  \refln{definition}{pactdef}{Define} \pactdefdef{.}
  \refln{definition}{trddef}{Define} \trddefdef{.}

  Now, suppose the \agt\ is following some \str\ $\STT$.
  \refln{definition}{rvst}{Define} \rvstdef{,}
  and \refln{definition}{rvat}{define} 
  \rvatdef{,}
  whose distributions are dependent on $\STT$.

  Let $\PSTT(\cdot)$ and $\ESTT[\cdot]$ denote the probability of an event
  and the expectation of a quantity
  when the \agt\ follows $\STT$.
  Because we follow a fixed \str, \refln{definition}{pact}{we know that}, \pactdeffull{,}
  \refln{proof}{actcind}{from which it follows} that
  $\AV_\TT$ is independent of all previous \til{}s and \act{}s given $\SV_\TT$.
  Notationally, \actcindpropfullg{\ctext{\refln{note}{implgen}{$\actcindprop$}.}}
  \refln{definition}{ind}{We know that} \inddef{,}
  and
  \refln{definition}{trd}{we know that} \trddef{,}
  \refln{definition}{trdcind}{where} \trdcinddef{.}

  We can now write the \trwd\ as
  $$\sum_{\TT = 1}^\T \RW(\SV_\TT)$$
  and the \atrwd\ as
  $$\ESTT\left[\sum_{\TT = 1}^\T \RW(\SV_\TT)\right].$$
\end{part}

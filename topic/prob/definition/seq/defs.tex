\def\seqdef#1{%
for \ctx\ $\SSPT{\SPT}{(\XV,\X)}$,
  \ctext{$\SQ$ denote the set of all \seq{}s,}
where ``$\SQQ$'' denotes an arbitrary such \seq.

Let $1 \le \TR < \infty$ denote an arbitrary \trl\ index#1%
}
